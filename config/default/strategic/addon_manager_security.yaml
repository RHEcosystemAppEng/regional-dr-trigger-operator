# This patch injects security configuration to the manager deployment
kind: Deployment
apiVersion: apps/v1
metadata:
  name: manager
spec:
  template:
    spec:
      containers:
      - name: manager
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - "ALL"
