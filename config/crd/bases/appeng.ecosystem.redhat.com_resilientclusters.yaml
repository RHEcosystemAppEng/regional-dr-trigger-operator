---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: resilientclusters.appeng.ecosystem.redhat.com
spec:
  group: appeng.ecosystem.redhat.com
  names:
    kind: ResilientCluster
    listKind: ResilientClusterList
    plural: resilientclusters
    shortNames:
    - rstc
    singular: resilientcluster
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ResilientCluster is used by the MultiCluster-Resiliency-Addon
          for maintain the status and state of each cluster running the Addon Agent.
          CRs should live in the relevant cluster-namespaces. One per Spoke, named
          after the cluster it represents.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ResilientClusterSpec defines the specification for creating
              a ResilientCluster
            properties:
              statusHistory:
                default: 1
                description: StatusHistory represents the number of previous ClusterStatus
                  CRs we should take into consideration for determining the Spoke
                  cluster status
                maximum: 5
                minimum: 1
                type: integer
            type: object
          status:
            description: ResilientClusterStatus defines the current status of the
              Spoke cluster.
            properties:
              currentStatus:
                description: ClusterStatus represents a status of the Spoke cluster
                  at a specific time
                properties:
                  availability:
                    description: ClustarAvailability is a bool representing whether
                      not the Spoke cluster is available
                    type: boolean
                  time:
                    format: date-time
                    type: string
                required:
                - availability
                - time
                type: object
              initialStatus:
                description: ClusterStatus represents a status of the Spoke cluster
                  at a specific time
                properties:
                  availability:
                    description: ClustarAvailability is a bool representing whether
                      not the Spoke cluster is available
                    type: boolean
                  time:
                    format: date-time
                    type: string
                required:
                - availability
                - time
                type: object
              previousStatuses:
                items:
                  description: ClusterStatus represents a status of the Spoke cluster
                    at a specific time
                  properties:
                    availability:
                      description: ClustarAvailability is a bool representing whether
                        not the Spoke cluster is available
                      type: boolean
                    time:
                      format: date-time
                      type: string
                  required:
                  - availability
                  - time
                  type: object
                maxItems: 5
                minItems: 1
                type: array
            required:
            - currentStatus
            - initialStatus
            - previousStatuses
            type: object
        required:
        - status
        type: object
    served: true
    storage: true
